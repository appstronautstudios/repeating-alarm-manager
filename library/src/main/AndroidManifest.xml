<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appstronautstudios.library">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <application
        android:allowBackup="true"
        android:supportsRtl="true">
        <!-- receivers -->
        <receiver
            android:name="com.appstronautstudios.repeatingalarmmanager.receivers.ReceiverNotification"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED" />
        <!-- Will not be called unless the application explicitly enables it -->
        <receiver
            android:name="com.appstronautstudios.repeatingalarmmanager.receivers.ReceiverDeviceBoot"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
